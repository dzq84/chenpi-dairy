{
  "name": "chenpi-dairy",
  "private": true,
  "version": "1.0.0",
  "description": "AI日记 - 心情记录与AI陪聊应用",
  "scripts": {
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix server run build",
    "start": "npm --prefix server run start",
    "postinstall": "npm --prefix server install"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
